---
title: C6284
ms.date: 11/04/2016
ms.topic: reference
f1_keywords:
- C6284
helpviewer_keywords:
- C6284
ms.assetid: f3633df6-2978-4899-8c0b-b495bd869e1a
author: mikeblome
ms.author: mblome
manager: markl
ms.workload:
- multiple
ms.openlocfilehash: 6bc0365bc84a30cd06f98b0326c7d21c74d762f9
ms.sourcegitcommit: 485ffaedb1ade71490f11cf05962add1718945cc
ms.translationtype: MT
ms.contentlocale: it-IT
ms.lasthandoff: 10/16/2019
ms.locfileid: "72447593"
---
# <a name="c6284"></a>C6284
avviso C6284: oggetto passato come parametro '% d'quando è richiesta una stringa nella chiamata a \<function >.

 Questo avviso indica che la stringa di formato specifica una stringa, ad esempio una specifica `%s` per `printf` o `scanf`, ma è C++ stato passato un oggetto.

 Questo difetto potrebbe produrre un output errato o arresti anomali.

 Questo messaggio viene spesso segnalato a causa del passaggio C++ di un oggetto che implementa un tipo stringa, ad esempio `std::string`, `CComBSTR` o `bstr_t`, in una chiamata di tipo C @no__t 4. A seconda dell'implementazione della C++ classe, ovvero se sono definiti gli operatori cast appropriati, C++ gli oggetti stringa possono spesso essere utilizzati in modo trasparente ogni volta che sono richieste stringhe C. Tuttavia, poiché i parametri per @no__t funzioni in stile 2 sono essenzialmente non tipizzati, non viene eseguita alcuna conversione in una stringa.

 A seconda dell'oggetto, potrebbe essere opportuno inserire un operatore `static_cast` nel tipo di stringa appropriato, ad esempio `char *` o `TCHAR``*`, oppure per chiamare una funzione membro che restituisce una stringa, ad esempio `c_str()`, nelle istanze di `std::string`.

## <a name="example"></a>Esempio
 Il codice seguente genera questo avviso perché un `CComBSTR` viene passato alla funzione `sprintf`:

```
#include <atlbase.h>
#include <stdlib.h>

void f()
{
  char buff[50];
  CComBSTR bstrValue("Bye");

  sprintf(buff,"%ws",bstrValue);
}
```

 Il codice seguente usa il cast statico per correggere questo avviso:

```
#include <atlbase.h>
#include <stdlib.h>

void f()
{
  char buff[50];
  CComBSTR bstrValue("Bye");

  sprintf_s(buff,50,"%ws",static_cast<wchar_t *>(bstrValue));
}
```

## <a name="see-also"></a>Vedere anche

- [Operatore static_cast](/cpp/cpp/static-cast-operator)
- [sprintf_s, _sprintf_s_l, swprintf_s, _swprintf_s_l](/cpp/c-runtime-library/reference/sprintf-s-sprintf-s-l-swprintf-s-swprintf-s-l)
